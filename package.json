{
  "name": "rythm-hacks",
  "private": true,
  "version": "1.0.0",
  "description": "Phase 1 monorepo for the Rythm rebuild",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace frontend\" \"npm run dev:backend\"",
    "dev:backend": "cd backend && python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 4000",
    "build": "npm run build --workspace frontend && npm run check:backend",
    "check:backend": "cd backend && python3 -m compileall app",
    "install:backend": "python3 -m pip install -r backend/requirements.txt"
  }
}
